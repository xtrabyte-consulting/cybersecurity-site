---
import Layout from '../layouts/Layout.astro';
import ServiceCard from '../components/ServiceCard.astro';
---

<Layout title="Home">
	<section class="hero">
		<video autoplay muted loop playsinline class="hero-video">
			<source src="/videos/hero/hero-loop.mp4" type="video/mp4" />
		</video>
		<div class="hero-overlay"></div>
		<div class="container">
			<div class="hero-content">
				<h1>Enterprise-Grade <span class="highlight">Cybersecurity</span> Solutions</h1>
				<p class="hero-subtitle">Protect your digital infrastructure with comprehensive security assessments, vulnerability management, and compliance testing.</p>
				<div class="hero-cta">
					<a href="/quote" class="btn-large btn-primary">Request a Quote</a>
					<a href="/services" class="btn-large btn-secondary">Our Services</a>
				</div>
			</div>
		</div>
	</section>

	<section class="services-preview">
		<div class="container">
			<h2 class="section-title">Our Core Services</h2>
			<p class="section-subtitle">Comprehensive security solutions tailored to your organization's needs</p>
			
			<div class="services-grid">
				<ServiceCard
					icon="🔍"
					title="CVE Vulnerability Scans"
					description="Comprehensive scanning and analysis of Common Vulnerabilities and Exposures across your infrastructure."
					image="/images/services/security-keys.png"
					link="/services#cve"
					features={[
						"Automated continuous scanning",
						"Detailed vulnerability reports",
						"Prioritized remediation guidance",
						"Real-time threat intelligence"
					]}
				/>
				
				<ServiceCard
					icon="🛡️"
					title="Penetration Testing"
					description="Simulated cyber attacks to identify and exploit vulnerabilities before malicious actors do."
					image="/images/services/penetration-testing.png"
					link="/services#pentest"
					features={[
						"Network and web application testing",
						"Social engineering assessments",
						"Executive summary reports",
						"Post-test remediation support"
					]}
				/>
				
				<ServiceCard
					icon="✅"
					title="EN 18031 Compliance"
					description="Ensure compliance with the latest EU RED Article 3.3(d,e,f) cybersecurity standards for internet connected devices."
					image="/images/services/en-compliance.png"
					link="/services#compliance"
					features={[
						"Gap analysis and assessment",
						"Thorough implementation testing",
						"Compliance roadmap development",
						"Evidence collection and reporting",
						"Ongoing compliance monitoring and support"
					]}
				/>
			</div>

			<div class="cta-section">
				<a href="/services" class="btn-primary">View All Services</a>
			</div>
		</div>
	</section>

	<section class="why-choose">
		<div class="container">
			<h2 class="section-title">Why Choose XtraByte?</h2>
			<div class="features-grid">
				<div class="feature feature-card" data-feature="team">
					<div class="feature-icon">🎓</div>
					<h3>Expert Team</h3>
					<p>Certified security professionals with decades of combined experience</p>
					<div class="feature-details">
						<p>Our team holds certifications including CISSP, CEH, OSCP, and more.</p>
					</div>
				</div>
				<div class="feature feature-card" data-feature="response">
					<div class="feature-icon">⚡</div>
					<h3>Fast Response</h3>
					<p>24/7 support and rapid incident response capabilities</p>
					<div class="feature-details">
						<p>Average response time under 30 minutes for critical incidents.</p>
					</div>
				</div>
				<div class="feature feature-card" data-feature="methods">
					<div class="feature-icon">🔒</div>
					<h3>Proven Methods</h3>
					<p>Industry-standard methodologies and cutting-edge tools</p>
					<div class="feature-details">
						<p>Following NIST, OWASP, and PTES frameworks with latest security tools.</p>
					</div>
				</div>
				<div class="feature feature-card" data-feature="reporting">
					<div class="feature-icon">📊</div>
					<h3>Clear Reporting</h3>
					<p>Actionable insights with detailed documentation</p>
					<div class="feature-details">
						<p>Executive summaries and technical reports tailored to your needs.</p>
					</div>
				</div>
			</div>
		</div>
	</section>

	<section class="cta-banner">
		<div class="container">
			<h2>Ready to Secure Your Infrastructure?</h2>
			<p>Get a comprehensive security assessment tailored to your needs</p>
			<a href="/quote" class="btn-large btn-primary">Request a Quote Today</a>
		</div>
	</section>
</Layout>

<style>
	.hero {
		padding: 6rem 0;
		position: relative;
		overflow: hidden;
		min-height: 600px;
		display: flex;
		align-items: center;
	}

	.hero-video {
		position: absolute;
		top: 50%;
		left: 50%;
		min-width: 100%;
		min-height: 100%;
		width: auto;
		height: auto;
		transform: translate(-50%, -50%);
		z-index: 0;
		object-fit: cover;
	}

	.hero-overlay {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: rgba(10, 14, 26, 0.3);
		z-index: 1;
	}

	.hero .container {
		position: relative;
		z-index: 2;
	}

	.hero-content {
		position: relative;
		max-width: 800px;
		text-align: center;
		margin: 0 auto;
	}

	.hero h1 {
		font-size: 3.5rem;
		line-height: 1.2;
		margin-bottom: 1.5rem;
		font-weight: 700;
	}

	.highlight {
		color: var(--secondary-color);
		position: relative;
	}

	.hero-subtitle {
		font-size: 1.25rem;
		color: var(--text-gray);
		margin-bottom: 2.5rem;
		line-height: 1.6;
	}

	.hero-cta {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
	}

	.btn-large {
		padding: 1rem 2.5rem;
		border-radius: 0.5rem;
		text-decoration: none;
		font-weight: 600;
		font-size: 1.1rem;
		transition: all 0.3s;
		display: inline-block;
	}

	.btn-secondary {
		background: transparent;
		border: 2px solid var(--secondary-color);
		color: var(--secondary-color);
	}

	.btn-secondary:hover {
		background: var(--secondary-color);
		color: var(--text-light);
	}

	.services-preview {
		padding: 6rem 0;
	}

	.section-title {
		text-align: center;
		font-size: 2.5rem;
		margin-bottom: 1rem;
		color: var(--text-light);
	}

	.section-subtitle {
		text-align: center;
		color: var(--text-gray);
		font-size: 1.1rem;
		margin-bottom: 4rem;
	}

	.services-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2rem;
		margin-bottom: 3rem;
	}

	.cta-section {
		text-align: center;
		margin-top: 3rem;
	}

	.why-choose {
		padding: 6rem 0;
		background: var(--dark-card);
	}

	.features-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
		margin-top: 3rem;
	}

	.feature {
		text-align: center;
		padding: 2rem;
		position: relative;
	}

	.feature-card {
		background: var(--dark-bg);
		border: 1px solid rgba(255, 255, 255, 0.1);
		border-radius: 1rem;
		cursor: pointer;
		transition: all 0.3s ease;
		overflow: hidden;
	}

	.feature-card:hover {
		transform: translateY(-5px);
		box-shadow: 0 10px 30px rgba(0, 163, 255, 0.3);
		border-color: var(--secondary-color);
	}

	.feature-card:hover .feature-icon {
		transform: scale(1.2) rotate(5deg);
	}

	.feature-icon {
		font-size: 3rem;
		margin-bottom: 1rem;
		transition: transform 0.3s ease;
		display: inline-block;
	}

	.feature h3 {
		color: var(--text-light);
		margin-bottom: 1rem;
		font-size: 1.3rem;
	}

	.feature p {
		color: var(--text-gray);
		line-height: 1.6;
	}

	.feature-details {
		max-height: 0;
		overflow: hidden;
		transition: max-height 0.3s ease, padding 0.3s ease;
		padding: 0 1rem;
	}

	.feature-card.active .feature-details {
		max-height: 200px;
		padding: 1rem;
		margin-top: 1rem;
		border-top: 1px solid rgba(0, 163, 255, 0.3);
	}

	.feature-details p {
		color: var(--secondary-color);
		font-size: 0.95rem;
		font-style: italic;
	}

	.cta-banner {
		padding: 6rem 0;
		background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
		text-align: center;
	}

	.cta-banner h2 {
		font-size: 2.5rem;
		margin-bottom: 1rem;
	}

	.cta-banner p {
		font-size: 1.2rem;
		margin-bottom: 2rem;
		opacity: 0.9;
	}

	@media (max-width: 768px) {
		.hero {
			min-height: 500px;
		}

		.hero h1 {
			font-size: 2.5rem;
		}

		.hero-subtitle {
			font-size: 1.1rem;
		}

		.section-title {
			font-size: 2rem;
		}

		.services-grid,
		.features-grid {
			grid-template-columns: 1fr;
		}
	}
</style>

<script>
	// Make feature cards interactive
	document.addEventListener('DOMContentLoaded', () => {
		const featureCards = document.querySelectorAll('.feature-card');
		
		featureCards.forEach(card => {
			card.addEventListener('click', () => {
				// Toggle active state
				const isActive = card.classList.contains('active');
				
				// Remove active from all cards
				featureCards.forEach(c => c.classList.remove('active'));
				
				// Add active to clicked card if it wasn't active
				if (!isActive) {
					card.classList.add('active');
				}
			});
		});

		// Smooth scroll for service card links
		document.querySelectorAll('a[href^="#"]').forEach(anchor => {
			anchor.addEventListener('click', function (e: Event) {
				const target = e.currentTarget as HTMLAnchorElement;
				const href = target.getAttribute('href');
				if (href && href.startsWith('#')) {
					e.preventDefault();
					const element = document.querySelector(href);
					if (element) {
						element.scrollIntoView({
							behavior: 'smooth',
							block: 'start'
						});
					}
				}
			});
		});
	});
</script>
